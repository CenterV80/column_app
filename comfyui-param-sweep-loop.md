# ComfyUIのパラメータ探索ループを作る：仕組みと作り方

*公開: 2026-09-19*

ComfyUIで動画や画像を生成していると、「cfgをいくつにするか」「stepsをどこまで落とせるか」を
延々と手で試すことになる。1本ずつ設定を変えて、生成して、見比べて、また変える。
この試行錯誤を、**生成はスクリプト、評価は人間、次の候補決めはClaude Code**に分担させる仕組みの作り方。

以下の例ではMiniMax H3の動画生成を題材にしているが、仕組み自体はモデルに依存しない。
ワークフローの中で「どのノードのどの入力を振るか」さえ決められれば、画像生成でも同じ形で回せる。

> 専門用語は初出の場所で説明しています。
> 末尾の「用語まとめ」は、その説明を1行に縮めた索引です。

---

## 1. 全体像

評価ループは**WebUIと切り離し、専用リポジトリで回す**。
（**リポジトリ** ＝ プロジェクトのファイル一式を、変更履歴ごと保管しておく置き場のこと）
WebUIが関わるのは、最初に設定を書き出すときと、最後に良い値を反映するときだけ。

```
WebUI                        base_workflow.json を書き出す
  ↓
sweep.py                     数値を差し替えてComfyUIに投入
  ↓
ComfyUI                      動画・画像を一括生成
  ↓
eval.html（人間）             スライダで評価
  ↓
Claude Code                  分析して次の params.json を作成
  ↓
収束した？ ── いいえ ──→ sweep.py に戻る
  │
  はい
  ↓
WebUIに反映
```

人間の出番は**評価と、最後の反映判断の2か所だけ**。
AIの絞り込みはClaude Codeが担当するので、ブラウザ側にAPIキーを置く必要がない。
評価ページは動画とスライダを並べるだけの、ただのローカルページで済む。

「**収束**」というのは、**これ以上パラメータをいじっても良くならなくなった状態**のこと。
ここに到達したらループを止めて、いちばん良かった値を採用する。

### 役割分担

| 担当 | 役割 |
|---|---|
| WebUI | ワークフロー（生成の設計図）の組み立てと書き出し |
| `sweep.py` | パラメータを差し替えてComfyUIへ投入、生成物を回収 |
| `server.py` + `eval.html` | 生成物とパラメータを並べ、人間がスライダで評価 |
| Claude Code | 評価の分析、次ラウンドの `params.json` 作成と実行 |

`.py` はPythonのプログラム（**スクリプト**＝決まった作業を自動でこなす小さなプログラム）、
`.html` はブラウザで開くページ、`.json` は設定やデータを書いておくテキストファイル。
拡張子を見れば、それが「動くもの」なのか「データ」なのかが分かる。

---

## 2. 1ラウンドの中身

上の図だけだと、`sweep.py` と `eval.html` の中身がブラックボックスのままになる。
ここでは**8本の動画を作って評価し終わるまで**を、ファイルの流れとして順に追う。

### 材料は2つ

ひとつめは `base/base_workflow.json`。WebUIから書き出した、生成の設計図。

```json
{
  "workflow": {
    "12": { "class_type": "KSampler",
            "inputs": { "cfg": 5.0, "steps": 30, "seed": 1234 } },
    "18": { "class_type": "SaveVideo",
            "inputs": { "filename_prefix": "sweep" } }
  },
  "param_map": {
    "cfg":   { "node": "12", "input": "cfg" },
    "steps": { "node": "12", "input": "steps" },
    "seed":  { "node": "12", "input": "seed" }
  },
  "values": { "cfg": 5.0, "steps": 30, "seed": 1234 }
}
```

- `workflow` — ComfyUIにこのまま送れる形の設計図。`"12"` や `"18"` はノードの番号で、
  `class_type` がそのノードの種類名（`KSampler` は生成の本体、`SaveVideo` は書き出し役）
- `param_map` — 「`cfg` という名前は、12番ノードの `cfg` 欄のこと」という対応表
- `values` — 書き出した時点の値。基準としてとっておく

ふたつめは `rounds/round_01/params.json`。**今回試す組み合わせの一覧**で、これはClaude Codeが作る。

```json
{
  "round": 1,
  "criteria": ["追従性", "動きの自然さ", "画質"],
  "items": [
    { "id": "001", "video": "videos/001.mp4",
      "params": { "cfg": 4.0, "steps": 30, "seed": 1234 } },
    { "id": "002", "video": "videos/002.mp4",
      "params": { "cfg": 4.0, "steps": 30, "seed": 5678 } },
    { "id": "003", "video": "videos/003.mp4",
      "params": { "cfg": 5.5, "steps": 30, "seed": 1234 } }
  ]
}
```

この例なら cfg を 4.0 / 5.5 / 7.0 / 8.5 の4通り、シードを2種。掛けて8本になる。
`criteria` は評価の観点で、あとで評価ページのスライダの項目になる。

### sweep.py が1本ぶんにやること

`items` を上から1件ずつ処理する。1件につき、次の5つ。

**① 設計図をコピーして数値を差し替える**

`base_workflow.json` の `workflow` をコピーし、`param_map` を引いて
「`cfg` → 12番ノードの `cfg` 欄」と辿り、そこに `4.0` を書き込む。
出力ファイル名（`filename_prefix`）も `001` と分かる名前に変えておく。
これをやらないと、8本すべてが同じ名前で出てきて区別できなくなる。

できあがったものを `rounds/round_01/workflows/001.json` として保存する。

**ここまでで止めるのが「dry-run」。**
dry-run（ドライラン）は、本番の処理を実行せずに手前までをやってみる「空(から)実行」のことで、
日本語でいえば「リハーサル」や「予行演習」が近い。
ここでは**動画の生成はせず、生成するはずだったファイルだけを作って**、
開いて「狙ったとおりの数値に書き換わっているか」を目で確かめる。

なぜやるのか。動画生成は1本あたり数分〜数十分かかる。
8本まとめて投げてから「指定した数値がまったく反映されていなかった」と気づくと、
その待ち時間がまるごと無駄になる。dry-runは数秒で済むので、
これを挟むだけで「1時間回してから全部やり直し」を防げる。

```
dry-run なし：  設定 → 生成（30分）→ 確認 → ミス発覚 → 最初から
dry-run あり：  設定 → 空実行（数秒）→ 確認 → 生成（30分）→ 完了
```

実行するときは、コマンドの末尾に `--dry-run` を付ける。この `--` で始まる部分は
**オプション**と呼ばれ、「いつもの動作を、この点だけ変えて実行して」というスイッチにあたる。
この手のツールで広く使われている書き方で、ComfyUI固有のものではない。

**② ComfyUIに送る**

`POST /prompt` に投げる。送る中身は `{"prompt": 差し替えた設計図, "client_id": "..."}`。
返ってくるのは `prompt_id` という**受付番号だけ**で、動画はここでは返らない。
ComfyUIは受け取ったジョブをキュー（順番待ちの行列）に積み、順に処理していく。

**③ 終わるのを待つ**

動画なら1本あたり数分〜数十分かかる。「終わったこと」を知る方法は2つある。

- **ポーリング** — `GET /history/<prompt_id>` を数秒おきに叩いて、結果が入ったかを見に行く
- **WebSocket** — `ws://<ComfyUIのアドレス>/ws?clientId=xxx` に繋ぎっぱなしにして、通知を待つ

**WebSocket**は「一度つないだら線を張ったままにして、向こうから勝手に知らせてくる通信」。
普通のWeb通信（HTTP）は、こちらから聞きに行ったときだけ1回返事が来て終わりなので、
完了を知るには何度も聞きに行くしかない。WebSocketならその必要がない。

| | たとえ |
|---|---|
| ポーリング | 店に「できました?」と5秒おきに聞きに行く |
| WebSocket | 呼び出しベルを渡されて席で待つ。鳴ったら取りに行く |

ComfyUIのWebSocketは完了だけでなく**進捗もリアルタイムで流してくる**
（今どのノードを処理中か、何ステップ目か）。ComfyUIの画面でプログレスバーが動くのはこれのおかげ。
`clientId` は通知の宛名で、`POST /prompt` に渡した `client_id` と同じ値にしておくと、
自分が投げたジョブの通知だけが届く。

ただし `sweep.py` は8本を順に流すだけなので、**ポーリングで十分**。実装も数行で済む。
WebSocketが効いてくるのは「あと何分で終わる」を画面に出したいときで、今回は優先度が低い。

**④ 出力ファイル名を聞く**

完了したら `GET /history/<prompt_id>` で「このジョブの出力は `sweep_00001.mp4`」
という情報が取れる。ComfyUI側の `output` フォルダに置かれたファイルの名前。

**⑤ 実体をダウンロードして置く**

`GET /view?filename=sweep_00001.mp4&type=output` で中身を取得し、
`rounds/round_01/videos/001.mp4` として保存する。
**`params.json` の `"video"` に書いた場所と一致させる**のが肝心で、
ここがずれると評価ページで動画が出てこない。

これを8回繰り返すと、フォルダはこうなる。

```
rounds/round_01/
  params.json              ← Claude Codeが用意した「試す組み合わせ」
  workflows/
    001.json 〜 008.json   ← sweep.py が作った、実際に送った設計図
  videos/
    001.mp4 〜 008.mp4     ← ComfyUIから回収した動画
```

「再実行で続きから回収する」というのは、`videos/003.mp4` がすでにあれば3本目を飛ばす、
という作りにしておくこと。途中で止めても最初からやり直しにならない。

### server.py がページを出す

`python server.py rounds/round_01` を実行すると、そのフォルダをブラウザに見せるだけの
小さなサーバーが立ち上がる。表示された `http://localhost:8000` のようなURLを開く。

**`localhost` は「今このパソコン自身」を指す住所**。
`http://` で始まるので外のインターネットに出ていきそうに見えるが、行き先は自分のPCの中で、
通信は一歩も外に出ない。他人からこのURLでアクセスされることもない。

後ろの `:8000` は**ポート番号**で、同じPCの中の「何番窓口か」を指す。
1台のPCで複数のプログラムがそれぞれ窓口を開けるようになっていて、
たとえばComfyUI自身は `8188` 番を使っている（`http://127.0.0.1:8188` などと表示されるあれ）。
`http://localhost:8000` は「このPCの中で動いている8000番の窓口につないで」という意味になる。

なぜサーバーが要るのか。`eval.html` をダブルクリックで直接開くと、
アドレス欄が `file://` で始まる。これは**サーバーを通さず、ファイルを直に開いた状態**で、
ブラウザの安全上の制限により**同じフォルダにある `params.json` すら読み込めず、
評価結果を書き出すこともできない**。

`http://localhost:8000` 経由なら、ブラウザは同じページを
「サーバーから受け取ったWebページ」として扱うので、どちらもできるようになる。
見た目は同じページでも、開き方でできることが変わる。

| 開き方 | アドレス欄 | params.json の読み込み | 評価の保存 |
|---|---|---|---|
| ダブルクリック | `file://...` | ✕ | ✕ |
| `server.py` 経由 | `http://localhost:8000` | ○ | ○ |

やることは2つだけ。

- `eval.html` / `params.json` / `videos/` を返す
- `eval.html` から送られてきた評価を `ratings.json` として保存する

### eval.html が動画とパラメータを並べる

ページを開くと、こう動く。

1. `params.json` を読み込む
2. `items` の1件につきカードを1枚作る
3. カードの中に3つ置く
   - **動画** — `<video src="videos/001.mp4" controls loop>`
   - **パラメータ** — `cfg 4.0 / steps 30 / seed 1234`（`params` をそのまま表示）
   - **評価スライダ** — `criteria` の項目ぶん（追従性・動きの自然さ・画質を各1〜5）とコメント欄
4. 人間が上から順に見て、スライダを動かす
5. 「保存」を押すと、入力した点数をまとめて `server.py` に送る → `ratings.json` ができる

**この「動画とパラメータを一緒に貼る」が、この仕組みの中心**。
動画を8本並べるだけなら見比べられるが、どれが cfg 4.0 だったのかが分からなければ、
評価は次の一手につながらない。数値と映像を同じカードに置くことで、
「cfgを下げると柔らかいが指示を無視しがち」という判断ができるようになる。

保存される `ratings.json` はこういう形になる。

```json
{
  "round": 1,
  "criteria": ["追従性", "動きの自然さ", "画質"],
  "ratings": [
    { "id": "001",
      "params": { "cfg": 4.0, "steps": 30, "seed": 1234 },
      "scores": { "追従性": 2, "動きの自然さ": 4, "画質": 3 },
      "mean": 3.0, "rated": true,
      "comment": "柔らかいが指示を無視ぎみ" }
  ]
}
```

`params` をここにもう一度書いておくのがポイント。
`params.json` と突き合わせなくても、**このファイル1つで「どの値が何点だったか」が読める**。
Claude Codeはこれを読んで次のラウンドを決める。

### ファイルの流れ

```
base_workflow.json ─┐
                    ├→ sweep.py ─→ workflows/00X.json ─→ ComfyUI ─→ videos/00X.mp4
params.json ────────┘                                                     │
     │                                                                    │
     └───────────→ eval.html（パラメータを表示）←──────────────────────────┘
                          ↓ 人間がスライダを動かす
                      ratings.json
                          ↓
                  Claude Codeが分析 → 次の params.json へ
```

---

## 3. リポジトリ構成

評価ループは**WebUIとは別リポジトリ**にする。
WebUIは製品、評価ループは調整用の道具と実験記録。分けておけば、試行錯誤でWebUI側のコードに手が入らない。

```
video-param-sweep/
  CLAUDE.md          ループのルールをAIに伝える
  PROGRESS.md        ラウンドごとの分析と判断の記録
  sweep.py
  server.py
  eval.html
  base/base_workflow.json   WebUIから書き出したもの
  rounds/
    round_01/  params.json, ratings.json, workflows/, videos/
    round_02/  ...
  .gitignore         rounds/*/videos/ を除外
```

`rounds/`（ラウンド）は**試行1回分のまとまり**。
「8本生成して、評価して、結果を見る」がひと区切りで、それを `round_01`、`round_02` と積み上げていく。

### 何をGitに入れるか

**Git**は、ファイルの変更履歴を記録しておく仕組み。
「**コミット**する」＝「この時点の状態を履歴として記録する」という意味で、
記録しておけば、あとから昔の状態を取り出したり、いつ何が変わったかを追える。

一方で、履歴に入れないほうがいいものもある。それを指定するのが `.gitignore` というファイル。

| 対象 | Git | 理由 |
|---|---|---|
| `videos/` | 除外 | 重い。`workflows/` から再生成できる |
| `params.json` / `ratings.json` | コミット | どの値で何点だったか、という履歴そのもの |
| `workflows/` | コミット | 実際に送ったワークフロー。良い結果を再現できる |
| `PROGRESS.md` | コミット | AIの分析と判断理由を後から追える |

生成物を捨ててパラメータと評価だけ残す、というのがポイント。
動画そのものはギガバイト級になるが、「どの数値で作ったか」は数十KBのテキストで済む。
その数十KBさえ残っていれば、あとから何度でも同じものを作り直せる。

---

## 4. 作り方

全8ステップ。ステップ1〜5が初回の準備で、6以降が繰り返しの運用になる。
Claude Codeに頼むステップには、**そのまま貼って使える依頼文**を載せた。
`〇〇` の部分は自分の構成に合わせて書き換える。

### ステップ1：WebUIに書き出しボタンを追加

今の設定を `param_map` 付きで `base_workflow.json` として保存できるようにする。
ここだけ**WebUIのリポジトリ側**での作業になる。

```
生成ボタンと同じ buildWorkflow() を使い、今の設定を
base_workflow.json として書き出すボタンを追加して。
- workflow（/promptに送るのと同じJSON）、param_map、values を含める
- param_map は 〇〇（例：cfg / steps / seed / output_prefix）を対象に、
  実際のノード番号と入力名で定義する
- 書き出し前に、参照画像の /upload/image を済ませる
- シードは確定した固定値で書き出す
```

`/prompt` や `/upload/image` は**エンドポイント**（外部のプログラムに頼みごとをする窓口のURL）。
前者が「この設定で生成して」の依頼窓口、後者が「この画像を先に預かって」の窓口にあたる。

### ステップ2：評価用リポジトリを作る

ここから先は**評価用リポジトリ**での作業。
`sweep.py` / `server.py` / `eval.html` と、`CLAUDE.md`・`.gitignore` を用意する。

依頼文には**2章で追った工程をそのまま仕様として書く**。
「セットアップして」だけでは、肝心の中身が埋まらない。

```
このリポジトリは生成パラメータの探索用。
CLAUDE.md（目的・手順・判断基準・データ形式の約束）と
.gitignore（rounds/*/videos/ を除外）を作って。

sweep.py を作って。やることは、params.json の items を1件ずつ:
  1. base_workflow.json の workflow をコピーし、param_map を引いて
     params の値を該当ノードの入力に書き込む。出力ファイル名も id 入りにする
  2. 差し替えたものを rounds/<round>/workflows/<id>.json に保存
     （--dry-run のときはここで終了）
  3. POST /prompt で投入して prompt_id を受け取る
  4. /history のポーリングで完了を待つ（進捗表示が要るならWebSocket）
  5. /history で出力ファイル名を取得し、/view で取得して
     rounds/<round>/videos/<id>.mp4 に保存
  ※ 既に videos/<id>.mp4 があるものはスキップ（再実行で続きから）

server.py と eval.html も作って。server.py は rounds/<round> を配信し、
eval.html からの保存を受けて ratings.json を書く。
eval.html は params.json を読んで1件1カードで表示し、
カードに「動画・パラメータ・criteriaぶんのスライダ・コメント欄」を並べる。
```

`CLAUDE.md` の中身は次章にテンプレートを置いた。

### ステップ3：ベースを書き出して配置

WebUIで基準の設定を作り、ステップ1のボタンで書き出したファイルを `base/` に置く。
ここは手作業。

### ステップ4：dry-runで確認

```
python sweep.py base/base_workflow.json rounds/round_01 --dry-run
```

`workflows/` の中身を開いて、狙ったノードの値が書き換わっているかをチェックする。
生成には進まないので数秒で終わる（2章の工程①を参照）。

この段階で `rounds/round_01/params.json` が要るので、先に作っておく。

```
base/base_workflow.json を使って、〇〇（例：cfg と steps）を振った
round_01 の params.json を作って。8本、シードは2種で固定。
そのあと sweep.py を --dry-run で実行して、差し替え結果を見せて。
```

### ステップ5：最初のラウンドを回す

dry-runで問題なければ `--dry-run` を外して本番実行。
生成が終わったら評価ページを開く。

```
python sweep.py base/base_workflow.json rounds/round_01
python server.py rounds/round_01
```

表示されたURLをブラウザで開き、上から順にスライダを動かして保存する。

### ステップ6：ループを回す（毎ラウンド）

ここからが繰り返し。毎ラウンド、これを貼るだけで済む。

```
最新ラウンドの ratings.json を分析して PROGRESS.md に追記し、
次ラウンドの params.json を作って。
内容を確認するので、sweep.py を実行する前に一度止めて。
ルールは CLAUDE.md に従うこと。
```

貼る文が毎回これで済むのは、**判断基準を次章の `CLAUDE.md` に全部押し込んである**から。
逆に言えば、`CLAUDE.md` に「どう分析して、次に何を試すか」が書かれていないと、
この4行だけでは回らない。毎ラウンド違う方針で動かれて、記録が比較できなくなる。

生成の手前で一度止めるのは、`sweep.py` を走らせるとGPU時間を使ってしまうため。
候補がおかしいまま8本流れ始めると、数十分ぶんが無駄になる。

承認したら生成し、`server.py` で評価する。これを収束まで繰り返す。

### ステップ7：最良の値を反映

収束したら、いちばん良かった値をWebUIの初期値やプリセットに反映する。
ここでこのループの目的は達成される。

### ステップ8：評価ページを育てる

使っていて不満が出たところを、1機能ずつ改修する。

```
eval.html を改修して。params.json / ratings.json の形式は変えないこと。
今回やりたいこと：〇〇（例：各カードに参照画像を並べて表示）
完了条件：サンプルで表示が崩れない、項目が無くてもエラーにならない
```

改修候補としては、参照画像との並列表示、2本を左右に並べる比較モード、
キーボードショートカット、前ラウンドの分析文の表示あたり。
何を評価しているかによって優先順位は変わる。
たとえば「元画像への追従性」を見ているなら、参照画像の並列表示が最優先になる。

---

## 5. CLAUDE.mdのテンプレート

`CLAUDE.md` は、**Claude Codeがそのリポジトリを開いたとき、最初に読む指示書**。
ここにループのルールを書いておけば、毎ラウンド同じ説明をし直さなくて済む。
新しく来た人に渡す作業手順書のようなもの、と考えると近い。

書くときに大事なのは、**項目を思いついた順に並べないこと**。
「手順」「判断基準」「禁止事項」「参照情報」は種類が違うので、混ぜると
読む側（人間もAIも）が「今どれを見ればいいのか」を見失う。
以下は6つのかたまりに分け、**手順を背骨にして、迷うところだけ外に出した**形にしてある。

| かたまり | 役割 |
|---|---|
| 目的 | 何のためのリポジトリか。1行でいい |
| 毎ラウンドやること | **手順**。ここだけ読めば流れが分かる。迷う箇所は下のかたまりを指す |
| 分析の基準 / 候補の決めかた | **判断基準**。手順の2と3で迷ったときに見る |
| 終了の条件 | いつ止めるか |
| やらないこと | **禁止事項**。手順と混ぜない |
| 参照：ファイルの形式 | **参照情報**。毎回読む必要はない、確認用 |

```markdown
# 目的
〇〇（例：MiniMax H3動画生成）のパラメータ探索。
人間がつけた評価をもとに、次に試す値を絞り込む。

# 毎ラウンドやること
1. 最新の rounds/round_XX/ratings.json を読む
   - rated: false が残っている、または videos/ に欠けがあれば、
     何もせず報告して止まる
2. 「分析の基準」に従って分析し、PROGRESS.md に追記する
3. 「候補の決めかた」に従って rounds/round_XX+1/params.json を作る
4. 内容を報告して止まる。承認を得てから次を実行する
   python sweep.py base/base_workflow.json rounds/round_XX+1
5. 「終了の条件」に当てはまったら、次を作らず「収束」と書いて終わる

# 分析の基準
- 総合スコア = 評価項目の重み付き平均。重みは〇〇（例：追従性2、他は1）
- 同じ値のものは、シード2種ぶんをまとめて平均する
- 評価項目ごとに傾向を見る。項目間にトレードオフがあれば両方書く
- 「cfg 4.0→5.5 で追従性+0.8／画質-0.4」のように、数値の変化として書く
- スコア差が 0.5 未満は「差なし」として扱う
  （人間のスライダ評価は同じ動画でもそれくらい揺れる。ノイズを改善と読まない）

# 候補の決めかた
- 前ラウンドで最良だった条件の周辺を、刻みを半分にして試す
- 効いていないと分かったパラメータは固定し、その枠を別のパラメータに回す
- 前ラウンドの最良条件を1本だけ再掲して混ぜる（比較の基準線にする）
- 1ラウンドは8本まで。動かすパラメータは3つまで、新しく増やすのは1つまで
- シードは 1234 / 5678 の2種で固定する
- パラメータ名は base_workflow.json の param_map にあるものだけ使う

# 終了の条件
- 総合スコアの最大値が、2ラウンド連続で +0.3 未満しか伸びなかった
- または round_〇〇（例：06）まで到達した

# やらないこと
- 承認を得ずに sweep.py を実行する
- eval.html / server.py / sweep.py を、頼まれていないのに変更する
- params.json / ratings.json の形式を変える

# 参照：ファイルの形式
- params.json: { round, criteria?, items: [{ id, video, params }] }
- ratings.json: { round, criteria, ratings: [{ id, params, scores, mean, rated, comment }] }
- PROGRESS.md の書式:
  ## Round XX
  - 試した値：
  - 結果（各項目の平均と総合スコア）：
  - 読み取れたこと：
  - 次にやること と その理由：
```

長く見えるが、**削るとステップ6の4行が回らなくなる**。内容として特に効くのは次の3つ。

**「差が0.5未満は差なし」** — 人間のスライダ評価は同じ動画でも揺れる。
閾値を決めておかないと、たまたまの +0.2 を「改善」と読んで、その方向に延々と進んでいく。

**「候補の決めかた」** — これが無いと、毎ラウンド違う方針で候補が出てくる。
刻みを半分にしていくのか、別のパラメータに移るのかが決まっていれば、
ラウンドをまたいで結果を比較できる。前ラウンドの最良条件を1本混ぜておくのは、
「前回の4.0」と「今回の4.0」が同じ点数になるかを見るため。
ここが大きくズレるなら、評価そのものがぶれている。

**「終了の条件」に数字を入れる** — 「伸びなければ」だけだと、+0.1 を伸びたと数えて永久に止まらない。
`+0.3 未満` と `round_06 まで` の二重で止める。前者は成果による終了、後者は時間切れによる終了。

なお `〇〇` になっているのは、**案件ごとに変わるので埋めてほしい2か所**（評価項目の重みと、
打ち切るラウンド数）だけ。それ以外は、そのまま使って構わない。

---

## 6. 注意点

- **データ形式は固定する** — `params.json` と `ratings.json` の形式が変わると、
  `sweep.py`・`eval.html`・Claude Codeの分析がすべてつながらなくなる。
  3つのプログラムが同じ書式のファイルを読み書きして連携しているので、
  どれか1つが勝手に書式を変えると、そこで鎖が切れる。
  改修を頼むときは毎回「形式は変えないこと」を添える
- **ファイル名の対応をずらさない** — `params.json` の `"video"` と、
  `sweep.py` が保存する先が一致していないと、評価ページで動画が表示されない。
  生成は成功しているのにページが真っ白、という場合はまずここを疑う
- **ノード構成を変えたら書き出し直す** — WebUIでワークフローの構造を変えたら
  `base_workflow.json` を再書き出しする。ノード番号がずれると、対応表が古いままになるため。
  存在しないノードは `sweep.py` の事前チェックで弾けるが、
  **別のノードに当たってしまうケースは検知できない**（番号としては存在するので、エラーにならない）
- **シードは固定** — **シード**は乱数の種で、同じ種なら同じ結果になるという性質がある。
  ここまで変えてしまうと、良くなったのがパラメータのおかげか、たまたま引きが良かっただけかを判別できない
- **1ラウンドの量** — 人間の評価疲れが一番のボトルネック。
  スライダ入力が5分で終わる8〜12本が目安
- **撤退条件** — 生成はGPU（画像や動画の計算を担当する部品）の時間を食う。
  時間はそのままコストなので、最大ラウンド数と収束条件を必ず決めておく
- **途中停止** — `sweep.py` は再実行で続きから回収する設計にしておく。
  ComfyUIを再起動すると**キュー**（生成の順番待ちの行列）が消えるので、
  そのとき投げ直すためのオプション（`--requeue` など）も用意しておくと楽
- **WebUIとつなぐのは後で** — チームがコードを触らずに回したくなった段階で、評価タブとして取り込む。
  その際にAPIキーの扱いと中継サーバーの要否を検討する

---

## 用語まとめ

本文中の説明を1行に縮めた索引。詳しくは、それぞれが初めて出てくる箇所を参照。

| 用語 | 意味 |
|---|---|
| ワークフロー | ComfyUIで「どの部品をどう繋いで生成するか」を書いた設計図。実体はテキストファイル |
| ノード | ワークフローの中の部品ひとつ。「モデルを読む」「絵を描く」など役割ごとの箱。番号で識別される |
| `class_type` | そのノードの種類名。`KSampler`（生成の本体）、`SaveVideo`（書き出し役）など |
| パラメータ | 生成時に指定する数値や設定。cfg・steps・seed など |
| cfg | プロンプトにどれだけ忠実に従わせるかの強さ。上げると指示に従うが硬くなり、下げると自由になるがズレる |
| steps | 生成の手数。多いほど丁寧だが時間がかかる |
| シード（seed） | 乱数の種。同じ種なら同じ結果になる。比較実験では固定するのが鉄則 |
| `param_map` | パラメータ名と「どのノードのどの項目か」の対応表 |
| dry-run | 本番の処理をせず手前までを実行する「空実行」。設定ミスを数秒で見つけるための事前確認 |
| オプション | コマンドの末尾につけて動作を変えるスイッチ。`--dry-run` など |
| スクリプト | 決まった作業を自動でこなす小さなプログラム。`.py` はPython製 |
| リポジトリ | プロジェクトのファイル一式を、変更履歴ごと保管しておく置き場 |
| Git / コミット | 変更履歴を記録する仕組みと、その記録操作。昔の状態に戻したり差分を追える |
| `.gitignore` | 履歴に入れないファイルを指定する設定ファイル |
| エンドポイント | 外部のプログラムに頼みごとをする窓口のURL。`/prompt` なら「生成して」の窓口 |
| `prompt_id` | ジョブを受け付けた証に返る番号。これを使って進捗や結果を問い合わせる |
| `localhost` | 「今このパソコン自身」を指す住所。通信が外のインターネットに出ていかない |
| ポート番号 | 同じPCの中の「何番窓口か」を指す番号。`:8000` や `:8188` の部分 |
| `file://` | サーバーを通さず、ファイルを直に開いた状態。ブラウザの制限が強くかかる |
| ポーリング | 「終わった?」と一定間隔で繰り返し確認しにいくやり方 |
| WebSocket | 一度つないだら線を張ったままにして、サーバー側から通知を流してもらう通信方式 |
| キュー | 生成の順番待ちの行列 |
| ラウンド | 試行1回分のまとまり。生成 → 評価 でひと区切り |
| 収束 | これ以上パラメータをいじっても良くならなくなった状態。ループの終了条件 |
| GPU | 画像・動画の計算を担当する部品。使った時間がそのままコストになる |
| `CLAUDE.md` | Claude Codeがリポジトリを開いたとき最初に読む指示書ファイル |

---

## まとめ

この仕組みは、3つの分担でできている。

**生成はスクリプトに** — `sweep.py` が設計図の数値を差し替え、ComfyUIに投げ、
出来上がった動画を回収して所定の場所に置く。人間が8回コピペする作業がなくなる。

**評価は人間に** — `eval.html` が動画とパラメータを同じカードに並べる。
人間がやるのはスライダを動かすことだけで、どの数値だったかを覚えておく必要はない。

**次の判断はClaude Codeに** — `ratings.json` を読んで次の候補を出す。
その判断基準は `CLAUDE.md` に書いてあるので、毎ラウンド説明し直さなくていい。

この3つが `params.json` と `ratings.json` という2つのファイルで繋がっている。
だから**形式を変えないことが一番の約束**になる。

そしてもうひとつ、**評価結果をGitに残していること**。
動画は捨てても、「どの値で何点だったか」は数十KBのテキストで残る。
これが積み上がっていけば、「このモデルではcfgは何あたりが良い」という知見が、
記憶ではなくファイルとして残る。
