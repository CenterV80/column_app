# ComfyUIフロントエンド開発のルール（CLAUDE.mdテンプレート）

*公開: 2026-09-18*

ComfyUIを叩く自作フロントエンドを作るとき、Claude Codeに渡す `CLAUDE.md` のテンプレート。
`〇〇` の部分はプロジェクトに合わせて書き換える。不要な項目は削除して構わない。

---

## 1. プロジェクト概要

- **目的** — 〇〇（例：チーム内で使うComfyUI動画生成UI）
- **利用者** — 〇〇（例：制作メンバー数名）
- **使用技術**
  - 言語：〇〇（例：TypeScript）
  - フレームワーク：〇〇（例：React）
  - ビルドツール：〇〇（例：Vite）
- **起動方法** — `〇〇`（例：`npm run dev`）
- **フォルダ構成**
  - `src/api/` — ComfyUIとの通信処理
  - `src/workflow/` — ワークフロー（JSON）の組み立て処理
  - `src/components/` — UIコンポーネント
  - `src/types/` — 型定義
  - 〇〇

---

## 2. ComfyUIとの連携

- **接続先** — 〇〇（例：`http://〇〇:8188`）
- **接続方式** — 〇〇（直接接続 / 中継サーバー経由）
- **基本方針** — ワークフローはフロントエンドで組み立て、ComfyUIは実行のみを担当する
- **使用するエンドポイント**
  - ジョブの送信 — `POST /prompt`
  - 進捗の受信 — `WebSocket /ws`
  - 生成物の取得 — `GET /view`
  - 画像のアップロード — `POST /upload/image`
  - ノード定義の取得 — `GET /object_info`
  - キューの確認 — `GET /queue`
- **使用しているカスタムノード**
  - 〇〇（ノードパック名） — 〇〇（バージョン / コミット）
- **使用モデル**
  - 〇〇（用途） — 〇〇（ファイル名）

---

## 3. ワークフロー生成のルール

- ワークフローは**API形式**で組み立てる
  - 各ノード：`"ノードID": { "class_type": "...", "inputs": { ... } }`
  - 接続：`["接続元ノードID", 出力インデックス]`
  - ノードIDは文字列
- 処理は部品ごとの関数に分け、`src/workflow/` に置く
  - 例：モデル読み込み / 条件付け / サンプリング / 出力
- 新しいノードを使うときは、`/object_info` で入力名・型・選択肢を必ず確認する。推測で入力名を書かない
- 生成したワークフローJSONを**画面で確認・ダウンロードできる機能**を維持する。この機能を削除・破壊しない
- `/prompt` のエラーレスポンス（`node_errors`）は画面に表示する

---

## 4. 安全面の禁止事項

- APIキー・パスワード・認証情報をフロントエンドのコードに書かない
- ユーザー入力をファイル名やパスにそのまま使わない
- ComfyUIの起動オプションやネットワーク設定を変更しない
- 外部サービスへの通信を勝手に追加しない
- 〇〇

---

## 5. コードの書き方

- 命名規則 — 〇〇（例：関数・変数はcamelCase、コンポーネントはPascalCase）
- 型定義 — `any` は極力使わず、ノードやワークフローには型を付ける
- エラーは握りつぶさず、必ずユーザーに分かる形で表示する
- WebSocketが切断された場合は再接続する
- 既存UIの見た目・操作感に合わせる
- 〇〇

---

## 6. Claude Codeへの作業ルール

- 複数ファイルにまたがる変更や大きな変更の前に、**計画を提示して確認を取る**
- 以下のファイル・フォルダは指示がない限り変更しない
  - 〇〇（例：`.env`、`public/`、ComfyUI本体のフォルダ）
- 変更後は、動作確認の方法（起動コマンド、確認手順）を示す
- 仕様が不明な点は推測で進めず、質問する
- 変更内容は、何をなぜ変えたかを簡潔に説明する

---

## 7. 既知の問題・メモ

困ったことや決めたことが出てきたら、ここに追記していく。

- 〇〇
