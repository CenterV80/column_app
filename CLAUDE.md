# column_app

## 記事まとめページ（articles.md / articles.html）の運用ルール

`articles.md` は、ユーザーが貼ったURL（または直接渡された文章）をもとに要約記事を作成し、
`articles.html` が動的に読み込んで一覧表示するページ。新しい記事はセクションの先頭（＝一覧の先頭）に追加する。

### 記事を書く前に

- **元のページを必ず読んでから書く。これは絶対のルール。** ユーザーからURLを渡されたら、まず必ず
  `WebFetch` でそのページ自体を読みにいく。検索結果の要約や記憶だけで記事を書き始めない。
- `WebFetch` で直接取得できない場合が多い（HuggingFace・多くの外部サイトがネットワークポリシーでブロックされる）。
  その場合は `WebSearch` を使うが、1回の検索結果の要約だけで済ませない。特に「最新のアップデート」
  「変更点」「既知の問題の修正」など、README概要だけでは拾えない情報がある可能性を疑い、
  更新履歴・Issue・Discussion・SNS投稿なども含めて複数回検索し、できる限り元ページの内容に近づける。
  過去に、リポジトリの直近アップデート（VRAM最適化、容量削減、バグ修正など）を一度の検索で見落とし、
  ユーザーから「要点ずれてません？」と指摘を受けたことがある。
- `github.com` は直接 `WebFetch` できることが多いので、GitHubリポジトリのURLは必ず`WebFetch`を試す。

### デプロイ前のファクトチェック（重要）

過去に、LTX-2.3の記事を書く際、MiniMax-H3の記事で調べた「Qwenテキストエンコーダ」の情報を
誤って混同し、「LTX-2.3はQwen系列を使っていた」という誤った記述をしてしまったことがある。
複数のモデル・製品を横断して記事を書いていると、こうした**取り違え**が起こりやすい。

これを防ぐため、記事をコミット・PR作成する前に以下を確認する：

1. **比較・対比の主張は両側を検証する** — 「〜とは異なり」「前世代の〜から」「初めて〜」といった
   比較表現を書く場合、比較対象（旧バージョンや競合製品）についても検索で裏付けを取る。
   会話内の記憶や別記事からの類推だけで書かない。
2. **数値・固有名詞は出典と突き合わせる** — パラメータ数、ファイルサイズ、倍率などの具体的な数値は、
   検索結果の文言をそのまま使い、記憶で言い換えない。
3. **製品間でスペックを取り違えていないか確認する** — このページはMiniMax-H3、LTX-2.3/2.5など
   複数の類似モデルを扱う。ある製品の技術情報（テキストエンコーダの種類、VRAM要件など）を、
   別の製品の記述に流用していないか、記事を書き終えたら見直す。
4. **確信が持てない場合は、断定を避けるか記述自体を省く** — 検索しても裏付けが取れない細部は、
   無理に具体化せず一般的な表現にとどめるか、その一文を削る。

上記を満たしてから `git add` → コミット → PR作成 → マージの通常フローに進む。
ユーザーから誤りの指摘を受けた場合は、リポジトリを検索し直して裏付けを取ってから訂正する。
