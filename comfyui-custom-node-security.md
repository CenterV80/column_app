# ComfyUIカスタムノードのセキュリティ、どうチェックする?

ComfyUIは豊富なカスタムノードのエコシステムが魅力の一つですが、その裏側には見落としがちなリスクがあります。カスタムノードは基本的に「起動時に自動実行される任意のPythonコード」であり、便利さと引き換えにセキュリティ上の注意点を抱えています。本記事では、カスタムノードを安全に扱うための考え方と、実際にチェックする仕組みについてまとめます。

## なぜカスタムノードは特にリスクが高いのか

ComfyUIのカスタムノードには、通常のPythonライブラリと共通する脆弱性リスクに加えて、いくつか固有の懸念点があります。

- **起動時の自動実行**: `custom_nodes`フォルダに置かれたパッケージの`__init__.py`は、ComfyUI起動時に自動的に読み込まれ実行されます。導入した時点でコードはすでに動いている、という点は見落としがちです。
- **pickle形式モデルの危険性**: `.ckpt`や`.pt`などpickleベースのモデルファイルは、読み込み時に任意コードが実行される脆弱性を含みうる形式です。一方`.safetensors`はデータのみを保持する形式で、この種のリスクがありません。
- **審査を経ていない配布**: ComfyUI Manager経由ではなくGitHubから直接cloneする場合、ノードはコミュニティによる審査を経ていないことが多く、信頼性の判断は導入者に委ねられます。
- **依存関係の衝突**: カスタムノードが要求するパッケージのバージョンが、既存環境の他のノードと衝突したり、意図しないパッケージを持ち込んだりすることがあります。

## チェックの2つの軸

セキュリティチェックは大きく「依存パッケージの脆弱性」と「コード自体の危険な処理」という2軸で整理すると見通しが良くなります。

### 依存パッケージの脆弱性スキャン

- **Python**: `pip-audit`(PyPA公式、既知CVEと照合)、`safety`
- **JS/Node**: `npm audit`(標準搭載)
- **横断的なツール**: `Snyk`、`OSV-Scanner`(Googleのオープンソース脆弱性データベースを活用)

### コード自体の静的解析

- **Python**: `bandit`(SQLインジェクションやハードコードされた認証情報などを検出)
- **汎用**: `Semgrep`(Python/JS両対応のルールベース解析)

これらに加えて、`socket.dev`のようなサプライチェーン専門のツールを使うと、パッケージの不審な挙動や権限拡大の兆候まで検知できます。typosquatting(似た名前の偽パッケージ)にも注意が必要です。

## 実践的なワークフロー

新しいカスタムノードを導入する際は、次のような手順を踏むとリスクを抑えられます。

1. **ComfyUI Managerを基本の導入経路にする**: Registry登録済みのノードはある程度のメタデータとレビューを経ています。ComfyUI Managerには既知の危険ノードのブラックリスト機能もあり、コミュニティで管理されています。
2. **導入前にコードを目視、またはスキャン**: 特に`__init__.py`と`requirements.txt`は要チェックです。`exec`、`eval`、`os.system`、`subprocess`、`pickle.load`といった呼び出しがないか確認します。
3. **safetensors優先**: pickle系モデルファイルの読み込みは可能な限り避ける設定にします。
4. **サンドボックスでの試用**: 本番環境に入れる前に、Docker等で隔離した環境で一度試すと安心です。
5. **依存関係の脆弱性チェック**: 各ノードの`requirements.txt`をまとめて`pip-audit`にかけます。

ただし、ブラックリスト照合は「すでに知られている悪意あるノード」の検出が中心で、未知のリスク(悪意はないが危険な処理を含む、脆弱な依存関係を持つ、など)まではカバーしきれません。ここを補うのが自作のスキャナーです。

## 自作スキャナーでできること

`custom_nodes`フォルダ全体、あるいはワークフローJSONで実際に使われているノードだけを対象に、次のチェックを自動化できます。

- **危険な関数呼び出しの静的検出**: AST(構文木)解析により、`exec`/`eval`/`os.system`/`subprocess`系/`pickle.load`/`torch.load`などの呼び出しをコメントの誤検知なく検出
- **ネットワーク通信の検出**: `requests`、`urllib`、`socket`などの import を洗い出し、外部通信の可能性がある箇所を可視化
- **依存関係の脆弱性チェック**: 全ノードの`requirements.txt`をまとめて`pip-audit`にかける
- **既知ブラックリストとの突合**: ComfyUI Managerが持つ危険ノードリストとフォルダ名を照合
- **リスクレベル付きレポート出力**: ノードごとに「危険」「中〜高」「低〜中」「低」を判定し、Markdownでまとめて出力

ワークフローJSONを渡せば、そこで使われている`class_type`(ノードのクラス名)も参考情報として抽出できます。ただし、`class_type`名とカスタムノードのフォルダ名が必ずしも一致するとは限らないため、最終的な対応関係の確認は目視が必要です。

## 静的解析の限界を忘れない

この種のスキャナーはあくまで一次スクリーニングです。動的に組み立てられた文字列や難読化されたコードは検出できませんし、「危険な呼び出しが見つかった」からといって即座に悪意があるとは限りません。モデルのダウンロード処理で`subprocess`を正当に使っているケースなども多く、検出結果は「目視確認が必要な候補」として扱うのが適切です。

最終的には、自動チェックで足切りをしたうえで、疑わしい箇所だけ人間が読む、という二段構えが現実的なバランスだと言えそうです。

## スキャナー本体をダウンロード

上記のチェックを自動化するPythonスクリプトを配布します。AST解析による危険呼び出し検出、ネットワークモジュールの検出、`pip-audit`連携、ComfyUI-Managerブラックリストとの突合をまとめて実行し、リスクレベル付きのMarkdownレポートを出力します。

<p style="text-align:center; margin: 24px 0;">
  <a href="scan_custom_nodes.py" download="scan_custom_nodes.py" style="display:inline-block; background:#6d5bd0; color:#fff; font-weight:700; text-decoration:none; padding:12px 28px; border-radius:999px; font-size:14.5px;">⬇ scan_custom_nodes.py をダウンロード</a>
</p>

**使い方**

```
# custom_nodes フォルダ全体をスキャン
python scan_custom_nodes.py --custom-nodes-dir /path/to/ComfyUI/custom_nodes

# ワークフローJSONで使われているノードのみに絞って参考情報を出力
python scan_custom_nodes.py --custom-nodes-dir /path/to/ComfyUI/custom_nodes --workflow workflow.json

# pip-audit / ブラックリスト取得をスキップ(オフライン環境向け)
python scan_custom_nodes.py --custom-nodes-dir /path/to/ComfyUI/custom_nodes --skip-pip-audit --skip-blacklist
```

デフォルトでは`security_report.md`にレポートを出力します。実行には標準ライブラリのみで動作しますが、依存関係の脆弱性チェックを行うには別途`pip install pip-audit`が必要です。

上記の「静的解析の限界」の節で触れた通り、このスクリプトの出力もあくまで一次スクリーニングです。「危険」判定が出たノードほど優先的に、実際のコードを目視で確認してください。
