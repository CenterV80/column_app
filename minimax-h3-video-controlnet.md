# 動画ControlNetのしくみ: 絵コンテ追従のために何が注入されているのか

*公開: 2026-09-15*

絵コンテへの追従を目的に、動画生成モデルのControlNetが「実際に何をしているのか」を整理したメモです。MiniMax H3のFun ControlNet Unionを題材に、ComfyUI本体の実装（`comfy_extras/nodes_minimax_h3.py`、`comfy/ldm/minimax/controlnet.py`）を読みながら、数値は一次情報に合わせて確認しました。

---

## 1. 基本の流れ

```
参照動画 / 絵コンテ
   ↓ 前処理（線画抽出など）
制御画像バッチ [F, H, W, 3]
   ↓ ノード内部でVAE encode
制御latent
   ↓ ControlNetがエンコード
特徴量（残差）
   ↓ DiTの指定ブロック出力に加算
生成に反映
```

**ノードに渡すのは画像バッチ**です。latentへの変換はノード側が引き受けるので、自分でフレーム数をlatent基準に直してから渡す必要はありません。`Apply MiniMax H3 Fun ControlNet`（`MiniMaxH3FunControlNetApply`）の`control_video`入力はIMAGE型で、内部で`vae.encode()`が呼ばれます。

### フレーム数がズレたらどうなるか

エラーにはなりません。実装では制御フレームの添字を生成フレーム数にクランプしているため、

- **制御フレームが足りない** → 最後の1枚がそのまま最後まで引き伸ばされる（ホールド）
- **制御フレームが多い** → 余りは切り捨てられる

解像度も`common_upscale`でbilinear＋centerに自動でリサイズされます。つまり「合わせなくても動く」のですが、意図せず末尾がホールドされ続けるので、**枚数は生成フレーム数に合わせるのが基本**です。

なお、H3の動画長は24fpsで「17の倍数＋5」フレームのグリッドに丸められます（既定は124フレーム＝約5秒）。他モデルの81フレームのような数字をそのまま持ち込むと、グリッドに合わずに丸められます。

---

## 2. 「注入」の中身

ベースモデルに入るのは画像そのものではなく、**ControlNetが出した中間特徴量（残差）**です。実装上は、指定ブロックの出力に対する一行の加算です。

```
out["img"].add_(skip, alpha=strength)
```

つまり、

```
ブロックn出力 + ControlNet出力[n] × strength → 次のブロックへ
```

DiTは全ブロックが同じ形`[N, D]`のデータを受け渡す残差ストリーム構造で、各ブロックはストリームを置き換えるのではなく差分を足しているだけです。**だから外部から同じ形で足し込めます。** モデル構造を壊さずに介入できるのは、この加算的な性質のおかげです。

### DiTブロックの中身（参考）

```
x ──┬─→ LayerNorm → Self-Attention ──→ (+) ──┐
    └────────────────────────────────┘      │
                                             ↓
    ┌─→ LayerNorm → Cross-Attention ─→ (+) ──┤   ← テキスト条件
    └────────────────────────────────┘      │
                                             ↓
    ┌─→ LayerNorm → FFN ────────────→ (+) ──┴─→ 出力
    └────────────────────────────┘
```

- Self-Attention … トークン同士を参照。時間方向の一貫性を担う
- Cross-Attention … テキスト埋め込みを参照。プロンプトが効くのはここ
- `(+)` … 残差加算

入力と出力の形が同じ`[N, D]`である点が要点です。

### 音声トークンには足さない

H3は動画と音声を同時に生成するモデルで、単一ストリームのTransformerの同じ列に音声トークンも乗っています。ControlNetの残差は加算の直前に音声トークンの位置をゼロで潰しているので、**制御が効くのは映像トークンだけ**です。ControlNetを強くかけても音声側の生成が直接引っ張られることはありません。

### 参照画像の行も対象外

R2V（Reference-to-Video）の参照画像やキーフレームとして条件付けされている行には、ゼロの制御行が入ります。**ControlNetは参照画像そのものを書き換えない**ので、「キャラの見た目は参照画像、構図はControlNet」という役割分担が成立します。

---

## 3. 層による効き方の違い

一般論として、DiTの層は深さによって担当が分かれます。

| 層 | 担当 |
|---|---|
| 浅い層 | 全体のレイアウト、構図 |
| 中間層 | 物体の形、意味的なまとまり |
| 深い層 | 質感、細部 |

絵コンテ追従で欲しいのは構図なので、**浅い〜中間層が主戦場**です。深い層まで強く効かせると、テクスチャまで線画に引っ張られて生成の自由度が落ちます。

H3 Fun ControlNet Unionはこれに対して、**全50ブロックのうち0 / 10 / 20 / 30 / 40番目の5箇所**に注入点を置く構成を取っています。浅い層から深い層まで等間隔に散らしてあり、ControlNet側の本体も対応する5ブロックだけです（先頭ブロックだけが入口の`before_proj`を持ち、各ブロックが出口の`after_proj`を持つ）。

ここは当初「Union系は層ごとに注入量を持っている」と理解していたのですが、実装を読むと違いました。**`strength`は5箇所すべてに同じ値がかかる単一のスカラー**で、層ごとに配分を変えるパラメータはありません。「層を絞る」操作はノードからは露出していないので、実質的に振れるのは`strength`と後述のステップ範囲です。

配分の話が出てくるのは、**複数のcontrol種類を重ねたとき**です。重ねた分だけ残差が合算されるため、strengthは共有の予算として扱う必要があります（depth 0.3＋pose 0.7は成立するが、両方1.0にすると破綻する、という報告が非公式ノードのREADMEにあります）。

---

## 4. タイムステップによる制御

ノイズ除去の初期ステップで大きな構造が決まります（進行度はAdaLNとしてブロック内のLayerNormに渡されます）。

`start_percent` / `end_percent`はsigmaに変換され、その区間に入っているステップでだけControlNetが動きます。区間外では制御ブロックの計算自体が走らないので、範囲を絞ると速度も少し稼げます。

| 設定 | 結果 |
|---|---|
| 前半だけ効かせる | 構図は固定、細部と動きは生成側に残る |
| 全ステップ効かせる | 固定されすぎて動きが死ぬ |

ComfyUI本体のノードの初期値は`strength` 1.0 / `start_percent` 0.0 / `end_percent` 1.0、つまり**既定では全ステップに最大強度でかかる設定**です。動きを残したいなら、ここは自分で下げる前提になります。非公式ノードのREADMEでは`end_percent`を1.0のままにせず0.6前後で打ち切る運用が勧められており、絵コンテ追従ならまずその辺りから振るのが良さそうです。

latent直書きで動きが止まるのは、この時間軸の分離ができず全工程で固定していたのに近い状態だからです。ブロック注入方式なら「レイアウトの傾向」として効くので、モーションは生成側に残ります。

---

## 5. 絵コンテ特有の課題：枚数が足りない

絵コンテはコマ単位なので、124フレームに対して数枚しかありません。埋め方の設計が必要です。

| 方式 | 挙動 |
|---|---|
| 同じ絵を保持、コマで切り替え | 構図は堅いがカット切替がカクつく |
| コマ間を補間して繋ぐ | 滑らかだが中間の構図が曖昧 |
| コマ位置だけ線画、間は空白 | 効かせたい所だけ効く。モーション自由度が残る |

1つ目は、実は**何もしなくてもそうなります**。前述のとおり、足りない分は最後のフレームがホールドされるためです。裏を返すと、2つ目・3つ目をやりたければ、制御動画は自分で124フレーム分組み立てて渡す必要があります。

3つ目が、latent直書きで動きが止まった経験を踏まえると相性が良さそうです。なお、フレーム軸に沿ってstrengthをフェードさせる`fade_in_frames` / `fade_out_frames`を追加するPRがComfyUI本体に出ていますが、2026年9月時点ではまだマージされていません（テスト追加待ち）。現状は制御画像側で作り込むことになります。

---

## 6. チャンネルの整理（混同しやすい点）

| 段階 | チャンネルの中身 |
|---|---|
| 入力画像 | RGB（3） |
| ControlNetヒント画像 | RGB（3、線画でも白黒をRGBに複製） |
| latent | 抽象的な特徴次元（H3は24ch、意味付けなし） |

ノードは`control_video[..., :3]`として先頭3chだけを読むので、**アルファ付きPNGを渡してもアルファは無視されます**。線画を透過で書き出している場合は、背景を焼き込んでから渡す必要があります。

latentのチャンネルに「明るさ」「エッジ」のような人間が読める対応はありません。学習で勝手に決まった次元です。明度 / 赤緑 / 黄青の分解はLab色空間（Photoshopのアレ）の話で、これとは別物です。エッジもチャンネルではなく、画像処理で後から検出して作るものです。

### VAEによる圧縮

H3のVisualVAEは**空間16×・時間4×・24チャンネル**（f16t4d24）です。

```
入力画像   [124, 768, 1344, 3]    ← RGB
   ↓ VAE encode
latent     [37, 48, 84, 24]       ← 空間1/16、24ch
   ↓ patchify 1×2×2 → DiTで処理
   ↓ VAE decode
出力画像   [124, 768, 1344, 3]
```

**空間だけでなく時間も圧縮される**点に注意してください。フレーム数とlatentフレーム数の対応は「17k+5 ↔ 5k+2」で、124フレームなら37です。さらにDiT側で1×2×2のパッチ化が入るため、空間方向の実効的な圧縮率は32×になります。

圧縮なのにチャンネル数が増える（3→24）のは、空間方向を削る代わりに各位置が持つ情報量を増やして辻褄を合わせているためです。

なお、ControlNet側の入力次元は49chです。内訳は「制御latent 24ch＋可視マスク 1ch＋マスク済み映像のlatent 24ch」で、後半2つは動画インペイント用です。ControlNetだけを使う場合は前半24chしか埋まりません。

---

## 7. 検証で振るべきパラメータ

- `strength` … 追従の強さ（既定1.0。複数control併用時は合算されるので予算配分が要る）
- `end_percent` … どのステップまで効かせるか（既定1.0。構図とモーションのトレードオフ）
- 線画の描き込み量 … 情報が多いほど固定されやすい
- コマの配置方法 … 上記5の3方式

感覚で決まらない領域なので、数パターン振って比較するのが早いです。

### 先に踏みそうな落とし穴

- **pruned版のベースモデルと、無印のControlNetを組み合わせるとエラーになります。** AdaLNの形式（curve基底か全幅か）が食い違うためで、ComfyUIも「ControlNetをベースモデルに合わせて変換しろ」と明示的に弾きます。軽量化した構成で使うなら、ControlNet側もpruned版にそろえる必要があります。
- Fun ControlNet Unionのテンプレートが最初のサンプリングステップで落ちる不具合が、Windows環境から報告されています（2026年9月時点で未解決）。`--disable-comfy-compiler`付きで起動すると最後まで完走するので、コンパイラ周りの相互作用が疑われています。

---

## 参考

- MiniMax H3 公式リポジトリ（VAE・Transformerの仕様）：https://github.com/MiniMax-AI/MiniMax-H3
- ComfyUI `comfy_extras/nodes_minimax_h3.py`（ノード定義・注入処理）：https://github.com/comfyanonymous/ComfyUI/blob/master/comfy_extras/nodes_minimax_h3.py
- ComfyUI `comfy/ldm/minimax/controlnet.py`（注入層・入力次元）：https://github.com/comfyanonymous/ComfyUI/blob/master/comfy/ldm/minimax/controlnet.py
- MiniMax-H3-Fun-Controlnet-Union：https://huggingface.co/alibaba-pai/MiniMax-H3-Fun-Controlnet-Union
- ComfyUI-H3-FunControl（非公式ノード、運用ノウハウ）：https://github.com/wyzborrero/ComfyUI-H3-FunControl
- ComfyUI PR #16283（フレーム軸のフェード、未マージ）：https://github.com/Comfy-Org/ComfyUI/pull/16283
- ComfyUI Issue #16230（サンプリング開始時に落ちる不具合）：https://github.com/Comfy-Org/ComfyUI/issues/16230
