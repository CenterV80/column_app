# MiniMax H3で絵コンテに追従させたい：参照画像・AddGuide・ControlNetの使い分け

絵コンテのコマ画像を渡して、そのとおりのレイアウトで動画を作りたい。ComfyUIで動画生成AI「MiniMax H3」を使ってこれを試していたところ、どうにも絵コンテへの追従が弱い。調べていくと、H3には画像の渡し方が3種類あり、それぞれ効くものがまったく違うこと、そして自分は使うルートを間違えていたことがわかりました。

この記事では、その整理と、いま採用しようとしている構成、そして「なぜその構成にしたのか」をまとめます。動画生成AIの仕組みに詳しくない人にも伝わるよう、前提から説明します。まだ検証中の部分もあるので、確定していない点はその旨を書いています。

---

## 前提：動画生成AIの中で起きていること（ざっくり）

以降の話を理解するために、4つの言葉だけ押さえておきます。

- **latent（潜在表現）**：AIが実際に扱う「縮小された設計図」です。動画をそのまま扱うとデータが大きすぎるので、VAEという仕組みで縦横・時間方向に圧縮してから処理し、最後に元の動画に戻します。
- **conditioning（条件付け）**：AIへの「指示書」です。プロンプトの文章や参照画像は、ここに変換されてAIに渡されます。
- **ステップ**：AIはノイズだらけの状態から少しずつノイズを取り除いて絵にしていきます。この1回1回がステップです。序盤のステップで大まかな構図が決まり、後半のステップで細部や質感が詰められます。
- **model（DiT）**：実際に絵を作る本体です。

建築にたとえると、latentが「図面」、conditioningが「施主の要望書」、modelが「職人」、ステップが「工程」です。序盤の工程で骨組みが立ち、後半で内装が仕上がる、というイメージを持っておくと、後の話がわかりやすくなります。

---

## 結論：画像の渡し方は3種類あり、効くものが違う

| ルート | 効くもの | 時間の指定 | どこに入るか |
|---|---|---|---|
| Ref2Vの参照画像 | 人物・物・画風などの「中身」 | できない | 参照ブロック |
| AddGuide | 指定したフレームの見た目 | 任意のフレームに置ける | conditioning（指示書） |
| Fun ControlNet | 構図・ポーズ・輪郭などの「構造」 | 毎フレーム指定 | model（職人側） |

---

## 1. なぜ参照画像ではダメだったのか

最初は、絵コンテのコマを「Ref2V（Reference to Video）」の参照画像として渡していました。

Ref2Vの参照画像は、プロンプト内で`<Picture 1>`のように呼び出す「素材集」のようなものです。AIはそこから「この人物」「この服」「この画風」といった中身を拾ってくれますが、画像ごとに「何秒目の画面か」という情報は持っていません。

つまり、絵コンテを参照に入れても、AIにとっては「こういう絵柄の素材がある」という情報にしかならず、「3秒目はこの構図で」という指示にはならないのです。追従が弱かったのは、失敗というより仕様どおりの挙動でした。

参照画像はキャラクターの見た目を保つのには強いので、**キャラシートは参照、絵コンテは別ルート**と役割を分けるのが出発点になります。

---

## 2. AddGuide：時間を指定して画像を置く

### できるようになったこと

以前のComfyUIのMiniMax H3対応では、キーフレーム（「このフレームはこの画像」という固定点）は、最初と最後のフレームにしか置けませんでした。モデル自体は途中のフレームにも対応できるため、その制限を外した`MiniMaxH3AddGuide`ノードが追加されました（ComfyUI v0.34.0に収録）。

- `frame_idx`（何フレーム目か）で置く位置を指定できる
- ノードを直列につなげば、何枚でも置ける

### なぜconditioning（指示書）に入れるのか

このノードは、画像を指示書に書き足す形で動きます。設計図（latent）は、動画の長さや解像度を確認するために読むだけで、書き換えません。

ここが大事なポイントです。以前、別のモデル（LTX）で、設計図に直接キーフレームを書き込む方法を試したところ、動画の動きが止まってしまいました。設計図に直接描き込むと、AIがその部分を「動かしてはいけないもの」と強く受け取ってしまいやすいのです。指示書に書き足す方式なら、AIは画像を「目標」として受け取りつつ、その前後の動きを自分で組み立てられるので、動きが死にくいと考えています。

### 注意：ラフをそのまま置くとラフに寄る

AddGuideは「このフレームはこの見た目」と指示するので、ラフスケッチを入れると、そのフレームがラフっぽい見た目に引っ張られる可能性があります。完成画に近いキーフレーム向きのルートと考えています（検証中）。

---

## 3. ControlNet：構図だけを効かせる

### ControlNetとは

ControlNetは、画像の「見た目」ではなく「構造」だけをAIに伝える仕組みです。絵コンテから輪郭線やポーズの骨格を取り出して渡すと、絵柄や色はAIに任せたまま、レイアウトだけを守らせることができます。ラフの絵柄に引っ張られない、というのがAddGuideとの一番の違いです。

MiniMax H3には「Fun ControlNet Union」があり、輪郭（Canny・HED・MLSD）、奥行き（Depth）、ポーズ（Pose）を1つのモデルで扱えます。ComfyUIが公式に対応しています。

### なぜmodel（職人）側に入るのか

意外だったのは、このControlNetが指示書ではなく、職人側（model）に「補助具」として取り付ける形で入ることです。指示書を書き換えるのではなく、絵を作る最中に職人の手元に「ここに線がある」という情報を直接届けるイメージです。

それでも設計図を直接いじるわけではないので、「動きを止めないよう、設計図には直接書き込まない」という方針とは矛盾しません。また、AddGuideは指示書、ControlNetは職人側と入り口が別なので、両方を同時に使っても配線がぶつかりません。

### 静止画だと動きが止まる問題

このControlNetは、1フレームごとに構造を指定する「コントロール動画」を前提にしています。絵コンテのコマを尺の分だけ繰り返した動画を作って入れると、構図はよく守られる代わりに、その間ずっと「同じ構造を守れ」と言われ続けるため、動きが止まってしまいます。

### 解決策：前半のステップだけ効かせる

前提の章で触れたとおり、構図は序盤のステップで決まり、後半は細部の仕上げです。そこで、

- 前半のステップ：ControlNetを付けた状態で生成する（骨組みを絵コンテどおりに立てる）
- 後半のステップ：ControlNetを外して生成する（動きや細部はAIに任せる）

と切り替えます。建築でいえば、骨組みを立てるときだけ図面どおりに固定し、内装の工程では職人の裁量に任せる形です。ComfyUIでは`SplitSigmas`というノードでステップを前後に分けられます。

何ステップ目で切り替えるかで「構図をどれくらい守るか」を調整できるので、WebUIではこれを「構図の追従」スライダーとして出す予定です。

---

## 4. 軽量化：int8 pruned版と8ステップ

MiniMax H3は非常に重いモデルです。実用的な速度で回すために、2つの軽量化を採用します。

### int8 pruned版のモデルを使う

- **int8**：モデル内部の数値を、より少ない桁数で保存した版です。ファイルサイズとVRAM使用量が大きく減る代わりに、わずかに精度が落ちます。
- **pruned**：実際の生成に使わない部分を取り除き、内部の構造を整理してスリム化した版です。

どちらもファイルは約21GBで、元の最高品質版（約66GB）よりずっと扱いやすくなります。

**ここで注意が必要なのが、ControlNetとの組み合わせです。** pruned版はスリム化の過程で内部の構造が変わっているため、元のモデル向けに作られたControlNetとは形が合いません。コンセントの形が違うプラグは差さらない、というイメージです。そのため、ControlNetも必ずpruned版に合わせて作り直された版（`minimax_h3_fun_controlnet_union_pruned_int8_convrot`）を使います。ComfyUIの公式テンプレートがちょうどこの組み合わせなので、そのまま流用できます。

### ステップ数を8に減らす

生成時間はステップ数にほぼ比例します。一般的な20ステップ前後から8ステップまで落としても品質が保てることを確認できたので、8ステップを標準にします。

ただし、前の章の「前半だけControlNetを効かせる」方法との兼ね合いがあります。20ステップなら切り替え位置を細かく選べますが、8ステップだと選択肢が1〜7しかなく、1ステップずらすだけで効き方が大きく変わります。そのため、構図が決まる序盤（1〜4ステップ目）を中心に、1ステップずつ試して最適な位置を探す予定です。

また、今使っているサンプラー（`res_multistep`）は、過去のステップの結果を参考にしながら次のステップを計算する方式です。途中で2つに分けるとその履歴がリセットされるため、ステップが少ないほど画質に影響が出やすい可能性があり、これも比較して確認します。

---

## 5. 補足：なぜH3はLTXより動きが止まりにくい（はず）なのか

前提の章で、latentは動画を時間方向にも圧縮した設計図だと書きました。この圧縮率がモデルによって違います。

- LTX：時間方向に8倍圧縮（設計図の1コマが、動画の8フレーム分）
- MiniMax H3：時間方向に4倍圧縮（設計図の1コマが、動画の4フレーム分）

キーフレームを1枚入れると、設計図の1コマ分に影響が及びます。LTXだと8フレーム分がまとめて固定されてしまいますが、H3なら4フレーム分で済みます。影響範囲が狭い分、キーフレームの前後で動きが死にくいと期待できます（ただし推測を含みます）。

その代わり、H3は設計図が細かい分だけ計算量が増えます。縦横の圧縮もLTXより控えめなため、同じ尺・同じ解像度で、AIが処理するデータ量はLTXの約8倍になります。H3が重い理由の一つがこれで、前の章の軽量化が必要になるわけです。

なお、途中でコミュニティのREADMEに「時間圧縮は約3.35倍」とあるのを見かけましたが、MiniMax公式のモデルカードでは4倍と明記されていました。3.35倍は出力フレーム数と設計図のコマ数から逆算した値のようで、公式仕様とは別物です。数値は一次情報で確認するのが大事だと改めて感じました。

もう一つ、H3の動画の長さは、24fpsで「17の倍数＋5」フレームの区切りにそろえられます。5秒を指定すると124フレーム（約5.17秒）になります。タイムラインのUIも、この区切りに合わせておく必要があります。

---

## 構成案のまとめ

| 役割 | 使うもの | なぜそれを選んだか |
|---|---|---|
| キャラの見た目を保つ | Ref2Vの参照画像（キャラシート） | 中身の抽出が得意。時間の指定は不要な情報なので相性がいい |
| 時刻ごとの見た目を決める | AddGuide（清書したキーフレーム） | 時間を指定でき、設計図を書き換えないので動きが止まりにくい |
| 構図を守る | Fun ControlNet（ラフから作ったコントロール動画） | 絵柄に引っ張られず、構造だけを伝えられる |
| 動きを殺さない | ControlNetは前半ステップのみ | 構図は序盤で決まるので、後半はAIに任せられる |
| 実用的な速度 | int8 pruned版＋8ステップ | VRAMと時間を大きく削れる。ControlNetもpruned版にそろえる |

もう一つ決めているルールとして、プロンプトに書くショットの時刻と、AddGuideの`frame_idx`は、必ず同じタイムラインのデータから自動で作るようにします。手で別々に入力すると両者がずれ、「プロンプトでは2秒目、画像では3秒目」のような矛盾した指示をAIに与えてしまうからです。

---

## これから検証すること

- ラフをそのままAddGuideに置いた場合と、清書したキーフレームを置いた場合の比較。追従が弱い原因が「ラフという絵柄」にあるのかを切り分けます。
- ControlNetを外すステップ位置（8ステップ中1・2・3・4・6・8）ごとに、構図の一致度と動きの量がどう変わるか。
- AddGuideの`frame_idx`が1フレーム単位で正確に効くのか、設計図の粒度（4フレーム前後）に丸められるのか。
- 参照画像とAddGuideを同時に使った場合の影響。種類の違う指示を同時に与えることになるため、結果が変わる可能性があります。

結果が出たら、続きとしてまとめる予定です。

---

## 参考

- MiniMax H3 モデルカード：https://huggingface.co/MiniMaxAI/MiniMax-H3
- ComfyUI PR #15439（MiniMaxH3AddGuide）：https://github.com/Comfy-Org/ComfyUI/pull/15439
- MiniMaxH3AddGuide ノードドキュメント：https://docs.comfy.org/ja/built-in-nodes/MiniMaxH3AddGuide
- MiniMax H3 Fun ControlNet Union チュートリアル：https://docs.comfy.org/tutorials/video/minimax/minimax-h3-fun-controlnet
